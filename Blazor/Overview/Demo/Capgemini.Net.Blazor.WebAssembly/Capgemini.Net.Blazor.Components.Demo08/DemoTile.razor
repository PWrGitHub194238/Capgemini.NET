@inherits DemoTileBase

<Tile Icon="@Icon"
      Title="@Title"
      Description="@Description"
      Href="@DemoTile.Href"
      PointCount="@PointCount"
      CompletedPointCount="@CompletedPointCount"
      TileOpened="@(() => TileOpened.InvokeAsync())"
      TileClosed="@(() => CloseTile(FullDemoTile.Context))">
</Tile>