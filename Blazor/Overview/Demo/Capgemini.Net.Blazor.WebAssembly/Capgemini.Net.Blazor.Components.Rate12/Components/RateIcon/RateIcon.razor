
<i class="@(Rate <= Container.TempRate ? ActiveCss : InActiveCss) @(Icon) cursor-pointer"
   alt="@Rate"
   @onclick="@(() => Container.ChangeProductRate(this))"
   @onmouseover="@(() => Container.ProductRateFocused(this))"
   @onmouseout="@(() => Container.ProductRateBlured(this))"></i>