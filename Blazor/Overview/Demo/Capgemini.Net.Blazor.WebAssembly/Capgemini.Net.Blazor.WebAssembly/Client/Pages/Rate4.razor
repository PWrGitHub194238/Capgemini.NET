@inherits Rate4Base

@for (int i = 0; i < @MaxRate; i += 1)
{
    var index = i;
    <i class="@(IsActive(index) ? Rate4Base.ACTIVE_STYLE : Rate4Base.INACTIVE_STYLE) @Icon cursor-pointer"
       @onclick="SetRate"
       @onmouseover="@(() => ShowRate(index))"
       @onmouseout="RevertRate"></i>
}