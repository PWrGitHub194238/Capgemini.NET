@inherits Rate6Base

@for (int i = 0; i < RateContext.MaxRate; i += 1)
{
    var index = i;
    <i class="@(IsActive(index) ? Rate3Base.ACTIVE_STYLE : Rate3Base.INACTIVE_STYLE) @(RateContext.Icon) cursor-pointer"
        @onclick="SetRate"
        @onmouseover="@(() => ShowRate(index))"
        @onmouseout="RevertRate"></i>
}